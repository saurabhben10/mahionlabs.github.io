---
interface Props {
  size?: 'sm' | 'md' | 'lg' | 'xl';
  showText?: boolean;
  className?: string;
}

const { size = 'md', showText = true, className = '' } = Astro.props;

const dimensions = {
  sm: { icon: 'w-6 h-6', text: 'text-base font-semibold' },
  md: { icon: 'w-8 h-8', text: 'text-lg font-bold' },
  lg: { icon: 'w-10 h-10', text: 'text-xl font-bold' },
  xl: { icon: 'w-14 h-14', text: 'text-2xl font-extrabold' },
};
---

<div class={`inline-flex items-center gap-3 select-none group ${className}`}>
  <!-- Vector SVG Logo: Classy Architectural Geometric 'F' Motif -->
  <div class={`relative ${dimensions[size].icon} flex items-center justify-center`}>
    <div class="absolute inset-0 bg-white/[0.03] rounded-xl group-hover:bg-champagne-400/[0.08] transition-colors duration-200"></div>
    <svg 
      viewBox="0 0 40 40" 
      fill="none" 
      xmlns="http://www.w3.org/2000/svg"
      class="w-full h-full relative z-10"
      aria-hidden="true"
    >
      <defs>
        <linearGradient id="flipedy-gold-grad" x1="0" y1="0" x2="40" y2="40" gradientUnits="userSpaceOnUse">
          <stop stop-color="#FFFFFF" />
          <stop offset="0.6" stop-color="#E5C07B" />
          <stop offset="1" stop-color="#D1C7BD" />
        </linearGradient>
      </defs>
      
      <!-- Outer Precision Border -->
      <rect 
        x="5" 
        y="5" 
        width="30" 
        height="30" 
        rx="8" 
        stroke="rgba(255,255,255,0.1)" 
        stroke-width="1.25" 
      />

      <!-- Architectural 'F' Path with Flip Horizon -->
      <path 
        d="M14 27V13H26M14 20H23" 
        stroke="url(#flipedy-gold-grad)" 
        stroke-width="2.5" 
        stroke-linecap="round" 
        stroke-linejoin="round"
      />

      <!-- Champagne Precision Accent Node -->
      <circle 
        cx="26" 
        cy="13" 
        r="1.75" 
        fill="#E5C07B" 
      />
    </svg>
  </div>

  {showText && (
    <span class={`tracking-tight text-white group-hover:text-champagne-100 transition-colors duration-200 ${dimensions[size].text}`}>
      Flipedy <span class="font-normal text-zinc-400">Labs</span>
    </span>
  )}
</div>
